apps:
  - tag: Payments
    product: payments
    paths:
      - /v1/payments
  - tag: Customers
    product: customers
    paths:
      - /v1/customers
  - tag: Preferences
    product: preferences
    paths:
      - /checkout/preferences
  - tag: Subscriptions
    product: subscriptions
    paths:
      - /preapproval
      - /preapproval_plan
  - tag: Merchant Orders
    product: merchant_orders
    paths:
      - /merchant_orders
  - tag: OAuth
    product: oauth
    paths:
      - /oauth/token
  - tag: Payment Methods
    product: payment_methods
    paths:
      - /v1/payment_methods
  - tag: POS
    product: pos
    paths:
      - /pos
  - tag: Orders
    product: orders
    paths:
      - /v1/orders
